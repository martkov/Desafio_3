import { StyleSheet } from 'react-native'

export default {
  primary: '#7fffd4',
  accent: '#CB807D',
  buttonTextColor: 'white',
  titleFontSize: 24
}

const globalStyles = StyleSheet.create({
  centered: {
    alignItems: 'center',
  }
})

export { globalStyles }